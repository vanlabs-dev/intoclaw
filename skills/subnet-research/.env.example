# Subnet Research — API Keys
# This skill borrows keys from chain-metrics and desearch.
# If you already have these keys set in those skill directories or at the workspace root,
# you do NOT need this file — the script will find them automatically.
#
# Only create a .env here if you want a self-contained setup:
# cp .env.example .env  and fill in the values.

TAOSTATS_API_KEY=
DESEARCH_API_KEY=
