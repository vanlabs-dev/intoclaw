# Subnet Research — Python dependencies
# Install with: pip install -r requirements.txt

requests>=2.28
Pillow>=10.0           # Header card PNG generation
matplotlib>=3.7        # Net flow chart generation (PNG output)
